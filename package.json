{
  "name": "sec-edgar",
  "version": "0.1.0",
  "description": "SEC EDGAR API CLI for AI agents",
  "bin": {
    "sec-edgar": "./index.ts"
  },
  "scripts": {
    "start": "bun run index.ts",
    "typecheck": "bun tsc --noEmit"
  },
  "dependencies": {
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.9.2"
  }
}
